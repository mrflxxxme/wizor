<!-- HEAD-SUMMARY (≤500т): Агрегированная память проекта (cross-role patterns/pitfalls), которую ведёт memory-curator на шаге 8 цикла. Per-role память — в .claude/agents/<role>/memory.md; индекс — в MEMORY-INDEX.md. Пусто до первой пройденной фазы. -->

# memory — агрегированная память WIZOR

Ведёт **`memory-curator`** (шаг 8 цикла фазы). Сюда попадают кросс-ролевые знания: подтверждённые паттерны (прошедшие ревью/аудит), pitfalls с правилом-предотвращением, калибровки. Узко-ролевые записи — в `.claude/agents/<role>/memory.md`. Recall — через `MEMORY-INDEX.md`.

**MUST-NOT:** секреты, полный код, чужой контент (только ссылки).

_Пусто — первая фаза ещё не пройдена._
