# VERIFY — P1 Foundation · 2026-06-24

**Verifier:** главная сессия (orchestrator) · **Фаза:** P1 (tier-4, infra) · **Ветка:** `claude/quirky-allen-meae1a`
**ADR-0018:** self-run тесты + live-gold (где возможно) перед PR; невозможное → явный `deferred_live_gold`.

## Tests

### Backend (Python 3.12, pytest)
- **Unit:** `pytest -m "not integration"` → **10 passed**, coverage **86.81%** (порог 70% — PASS).
  - `test_health.py` (AC-4: /health с/без/невалидным X-Tenant-Id), `test_config.py` (NFR-1: РФ-резидентность, reject us/eu), `test_analytics.py` (PostHog no-op + контракт событий), `test_worker.py` (Celery ping eager), `test_metrics.py` (Prometheus exposition).
- **Lint/format:** `ruff check .` + `ruff format --check .` → All checks passed.
- **Types:** `mypy src` (strict) → Success, 17 файлов, 0 ошибок.
- **SAST:** `bandit -q -r src` → 0 issues.

### Frontend (Node 22, vitest)
- **Unit:** `npm test` → **6 passed**, coverage **100% lines** (порог 70% — PASS).
- **Lint:** `npm run lint` (eslint) → No warnings or errors.
- **Types:** `tsc --noEmit` → OK.
- **Deps:** `npm audit` → 0 high / 0 critical (2 moderate + 2 low транзитивные в next/eslint; CI блокирует `--audit-level=high`).

## Live-gold

**Решение (грилл):** живой smoke стека идёт в **CI** (GitHub Actions), т.к. Docker-демон в этой сессии недоступен (`/var/run/docker.sock` отсутствует — подтверждено). CI-лог = `evidence_url`.

CI `backend.yml::integration` (services: pgvector/pg16 + redis7) выполняет реальный live-gold:
- `alembic upgrade head` → миграция применяется к живому Postgres (migration-safety + pgvector).
- `python -m wizor.iam.seed` → идемпотентный сид тестового тенанта.
- `pytest -m integration --no-cov`:
  - **AC-8:** `vector_dims('[1,2,3]'::vector) == 3` (pgvector активен).
  - **AC-6:** `count_foreign_tenant_sites() == 0` (изоляция) + тест со вторым тенантом (фильтр не протекает).
  - FR-P1-2: сид-тенант присутствует.
- **AC-7:** живой Celery worker + `celery inspect ping` (loop до успеха).

CI `backend.yml::keycloak-smoke`: реальный Keycloak 26 `start-dev --import-realm`, health=UP на :9000, realm `wizor-dev` доступен через `.well-known/openid-configuration`.

CI `frontend.yml`: eslint + tsc + vitest(+coverage) + `next build` + `npm audit --audit-level=high`.
CI `security.yml`: gitleaks (secrets) + trivy (HIGH/CRITICAL, exit 1).

> Статус прогона CI на финальном коммите — см. ссылку в гейте `P1-foundation.md` (`evidence_url`).

## deferred_live_gold (честно, не тихо — ADR-0018)

| # | Что отложено | Reason | Founder action |
|---|---|---|---|
| DLG-1 | **PostHog self-hosted UI** (AC-5, FR-P1-1 «событие видно в UI») | Тяжёлый ClickHouse-стек вне bootstrap-бюджета P1; SDK интегрирован обеими сторонами с capture-абстракцией, no-op-режим оттестирован. | Поднять self-hosted PostHog + `POSTHOG_API_KEY` на проде/P7; проверить North Star событие в UI. |
| DLG-2 | **`make dev-bootstrap` ≤600с локально** (AC-1) | Docker-демон недоступен в эфемерной сессии (нет DinD). Compose валиден (`docker compose config` PASS), healthchecks/`--wait` заданы. | Прогнать `make dev-bootstrap` на founder-машине с Docker; зафиксировать время. |

Оба — задокументированы в коде/`.env.example` и в гейте; включение тривиально (смена ключа / наличие Docker). Соответствует ADR-0018 (явный deferral).
